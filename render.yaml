services:
  - type: web
    name: terrasnap
    env: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn app:app
    autoDeploy: true
    branch: main
    envVars:
      - key: PORT
        value: 10000
